FROM node:20-alpine
WORKDIR /app

# optional: speed up installs in containers
ENV CI=1

# install deps when container starts (dev convenience)
CMD sh -c "corepack enable && pnpm i && pnpm next dev -p 3000 -H 0.0.0.0"
EXPOSE 3000